# ── Runtime ──────────────────────────────────────────────
# (no secrets needed yet — add these when you connect an email provider)

# Email provider API key (e.g. Resend, Loops, SendGrid)
# EMAIL_API_KEY=

# Optional: override the public URL
# NEXT_PUBLIC_SITE_URL=https://nyxethos.com
